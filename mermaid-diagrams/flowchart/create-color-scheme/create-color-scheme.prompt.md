modify the color key so that it has a {descriptors} color scheme composed of {desired_seed_colors}. add new categories as needed 

wording isn't relevant, only the color palette matters {relevant_context}